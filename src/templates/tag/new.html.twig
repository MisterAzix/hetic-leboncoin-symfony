<!DOCTYPE html>

<title>New Tag</title>

{% block body %}
    <h1>Create new Tag</h1>

    {{ include('tag/_form.html.twig') }}

    <a href="{{ path('app_tag_index') }}">back to list</a>
{% endblock %}
