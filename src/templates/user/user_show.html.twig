{% extends 'base.html.twig' %}

{% block body %}

    <div class="p-5 mb-4 bg-light rounded-3">
        <div class="container-fluid py-5">
            <h1 class="display-4">Information Utilisateur</h1>
            <p class="col-md-8 fs-4">Nom : {{ user.firstName }}</p>
            <p class="col-md-8 fs-4">Prénom : {{ user.name }}</p>
            <p class="col-md-8 fs-4">Email : {{ user.email }}</p>
            <p class="col-md-8 fs-4">Date de creation : {{ user.createDate|date('d F Y') }}</p>
            <a href="{{ path('app_user_modify', { email: user.email }) }}">
                <button class="btn btn-primary" type="button">Modifier</button>
            </a>
            <a href="{{ path('app_user_delete_api', { email: user.email }) }}">
                <button class="btn btn-danger" type="button">Supprimer</button>
            </a>
        </div>
    </div>

{% endblock %}