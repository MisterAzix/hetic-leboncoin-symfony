{% extends 'base.html.twig' %}

{% block title %}Edit Ad{% endblock %}

{% block body %}
    {% include 'partials/navbar.html.twig' %}

    <div class="h-75 d-flex flex-column justify-content-center">
        <h1 class="text-center py-3">Modifier mon annonce</h1>

        <div class="row">
            <div class="col-4 m-auto">
                {{ include('ad/_form.html.twig', {'button_label': 'Modifier mon annonce'}) }}

                <div class="d-flex justify-content-center">
                    <a href="{{ path('app_ad_index') }}" class="btn btn-link rounded-0">back to list</a>
                </div>

                {{ include('ad/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
